<h2>Articles</h2>
<ul>
  <% if @articles.flatten.count > 0 %>
    <% @articles.flatten.each do |a| %>
    <li><%= link_to a.title, "http://#{a.url}" %></li>
    <% end %>
  <% else %>
    <%= link_to 'Create New Article', new_collection_article_path(1) %>
  <% end %>
</ul>
