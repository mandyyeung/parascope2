<%= form_for(@article) do |f| %>
  <%= f.label :url %>
  <%= f.text_field :url %>
  <%= f.label :title %>
  <%= f.text_field :title %>

  <%= f.collection_select(:collection, :collection_id, Collection.all, :id, :name) %>

<% end %>
