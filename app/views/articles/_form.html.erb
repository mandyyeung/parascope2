<%= form_for([current_user, @collection, @article]) do |f| %>
  <%= f.label :url %>
  <%= f.text_field :url %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <%= f.collection_select(:collection_ids, Collection.all, :id, :name) %>
  <%= f.submit %>

<% end %>
